server:
  # 启动端口
  port: 8888

spring:
  # 配置mysql数据库
  datasource:
    url: jdbc:mysql://192.168.65.128:3306/springSecurity?characterEncoding=utf-8&serverTimezone=UTC&useSSL=false
    username: root
    password: 190715zcy
    driver-class-name: com.mysql.jdbc.Driver
  # 配置redis
  redis:
    host: 192.168.65.128
    port: 6379
    password: 190715zcy

mybatis-plus:
  # 配置mapper的xml映射文件位置
  mapper-locations: classpath*:/mapper/**/*.xml

